name: besmart_2025
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  google_sign_in: ^6.2.1
  model_viewer_plus: ^1.7.2
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rainbow_emoji_blob.glb
    - assets/Hamburger.glb
    - assets/Robot.glb
    - assets/Planets.glb
    - assets/Swap-removebg-preview.png
    - assets/icon.glb
    

